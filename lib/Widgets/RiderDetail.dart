import 'package:flutter/material.dart';

class RiderDetail extends StatelessWidget {
  const RiderDetail({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
